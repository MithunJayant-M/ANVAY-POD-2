import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  standalone: true,
  template: '<p>Login Page (Work in Progress)</p>',
})
export class LoginComponent {} // The "export" is what makes it a module